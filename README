На Си, без использования готовых функций из библиотек (atoi(),
 printf() и подобных).

 1. Реализовать функцию преобразующую число в строку.
 size_t num_to_str(uint64_t num, char *str, size_t str_size)
 На входе:
 - некоторое число uint64_t num
 - буфер для сохранения результата
 - размер буфера

 На выходе:
 - данные в буфере
 - вернуть количество байт записанных в буфер

 2. Реализовать функцию преобразующую число в строку.
 uint64_t str_to_num(const char *str, size_t str_len)
 На входе:
 - буфер со строкой
 - размер буфера

 На выходе:
 - вернуть число

 В обоих случаях десятичная система.


 